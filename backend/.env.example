# Application
PORT=3001
NODE_ENV=development

# CORS: allowed frontend origin
FRONTEND_URL=http://localhost:3000

# Database (URL-encode special chars in the password, e.g. @ -> %40)
DATABASE_URL="postgresql://postgres:password@localhost:5432/zento?schema=public"
